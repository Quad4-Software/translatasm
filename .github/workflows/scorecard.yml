# Pinned actions (bump tag and SHA together when upgrading):
#   actions/checkout@v7.0.1                 3d3c42e5aac5ba805825da76410c181273ba90b1
#   actions/upload-artifact@v7.0.1           043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
#   ossf/scorecard-action@v2.4.4            2d1146689b8cda280b9bc96326124645441f03bc
#   github/codeql-action/upload-sarif@v4.37.9 cdf488f595d80d6e07e03d4674febd5ab45fa938
#
# Hardens supply-chain checks against pwn-request / tag-move / secret-dump
# patterns (tj-actions, prt-scan). No pull_request_target. SHA-pinned only.
# https://docs.github.com/en/actions/reference/security/secure-use
# https://securitylab.github.com/research/github-actions-preventing-pwn-requests/

name: OpenSSF Scorecard

on:
  branch_protection_rule:
  schedule:
    - cron: "17 5 * * 1"
  push:
    branches:
      - master
  workflow_dispatch:

permissions: read-all

jobs:
  analysis:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run analysis
        uses: ossf/scorecard-action@2d1146689b8cda280b9bc96326124645441f03bc # v2.4.4
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: SARIF file
          path: results.sarif
          retention-days: 5

      - name: Upload to code-scanning
        uses: github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          sarif_file: results.sarif
